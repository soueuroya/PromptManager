# PromptManager

PromptManager is a VS Code extension workspace for organizing AI development
tasks, prompts, acceptance criteria, references, and execution queues.

The repository root contains project documentation and PromptManager workspace
state. The extension project lives in `prompt-manager/`.

## Development

Run extension commands from the nested extension folder:

```bash
cd prompt-manager
npm install
npm run compile
vsce package
```
